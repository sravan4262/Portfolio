Voice clips for each pipeline stage (click-to-play, never autoplay).

Live files (committed): init, education, accenture, upmc, citrix,
byteai, infinity, truespend, skills, cleanup  — all `.m4a` (AAC).

Source recordings live in /Memos (*.qta from Voice Memos) and were converted with:
  afconvert -f m4af -d aac "Memos/<Name>.qta" "public/audio/<id>.m4a"

Scripts / transcripts are in _docs/recording-scripts.md and src/content/stages.ts.
